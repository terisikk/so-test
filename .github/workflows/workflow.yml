name: Continuous Delivery

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: "Print shell"
        shell: cmd
        run: |
          C:\Windows\system32\cmd.exe /c echo $0
          echo $0
          ver
          uname -a
          env
          
