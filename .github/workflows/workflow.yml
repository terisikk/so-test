name: Continuous Delivery

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: "Print shell"
        shell: cmd
        run: |
          uname -a
          env
          
